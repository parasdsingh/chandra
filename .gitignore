target/
node_modules/
dist/
.DS_Store
*.log
tools/swetest
worker/node_modules/
worker/.wrangler/
# Belt and braces: `tools/release.sh` passes `--cwd worker` so wrangler keeps
# its state there, but a hand-run command from the root would put it here.
.wrangler/
