{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the panel and settings windows. Deliberately minimal: the front end reaches everything through this crate's own commands, so no filesystem, shell, http or dialog permission is granted.",
  "windows": [
    "panel",
    "settings"
  ],
  "permissions": [
    "core:default"
  ]
}
